# Backend service base URLs (no trailing slash). Defaults match docs/API_CONTRACTS.md §2.
# Copy to `.env` and point these at your running services (or the gateway once it lands).
VITE_USER_API=http://localhost:8081
VITE_TASK_API=http://localhost:8082
VITE_GOAL_API=http://localhost:8083
